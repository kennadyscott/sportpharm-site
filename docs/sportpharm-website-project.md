---
name: sportpharm-website-project
description: "New SportPharm website build; custom full-stack, page by page"
metadata: 
  node_type: memory
  type: project
  originSessionId: 8c5f1483-3c92-4f1e-ab35-614dc81c9371
  modified: 2026-07-24T18:32:20.741Z
---

Building a new website for **SportPharm** — a sports-injury recovery brand: educational guidance + solutions/products helping athletes recover and return to play. NOT product-first; the whole site is framed problem/solution (browse by injury/body part/sport). President is **Brandon Welch** (featured "Ask Brandon" expert persona). Tagline: "We Take Our Drugs Seriously." Started 2026-07-06.

Brand: SportPharm red (~#E0312A) on dark navy (#0A1522 / #070F19), white text. **REAL LOGO now in use** (user provided files in sportpharm-assets): main logo = "Sportpharm text & tag logo 300.png" (transparent, red wordmark + black outline + black "We Take Our Drugs Seriously." tagline). Copied to site as **assets/logo.png** (original, for LIGHT backgrounds). Generated **assets/logo-white.png** via PIL (black→white recolor, keeps red) for DARK backgrounds. Homepage header+footer use logo-white; sports-medicine header uses logo.png, its dark footer uses logo-white. **HOMEPAGE LOGO (current):** SportPharm WORDMARK + small tag underneath "Sports Recovery and Performance Hub" (per user; replaced the earlier "Recovery by SportPharm" variant). Made **assets/wordmark-white.png** by PIL-cropping logo-white.png to remove its baked-in "We Take Our Drugs Seriously." tagline (kept just the SPORTPHARM wordmark, white/red). Header + footer render: wordmark img (height 31/30px) + `.brand-sub` span (Roboto Slab, white, "Sports Recovery and Performance Hub"). (recovery-by-sportpharm-logo.png / logo-recovery-white.png no longer used but still in assets.) Sports Medicine page keeps the full main logo (logo.png).

**HOMEPAGE NAV (restructured):** plain text items **Injuries · Recovery · Performance · Learn** (each with dropdown), then a divider (border-left), then two CAPSULES: **The Pharmacy** (red capsule; REPLACED "Products"; now a DIRECT link to https://sportpharm.com/store/ in a new tab — dropdown/caret removed per user) and **Sports Medicine** (white capsule → sports-medicine.html). **Learn dropdown** includes "Sports Rx Network ↗" → https://sportsrxnetwork.com/ (always new tab). Removed the old "Sports Medicine" and "Products" text items and the nav-right pharmacy-pill. Search + cart icons remain on the right. Mobile menu updated to match.
**SPORTS MEDICINE PAGE NAV:** **"← Sports Recovery Hub"** navy-outline capsule (→ index.html) + The Pharmacy (red). Professional Login REMOVED per user (HCPs don't need a login for now). Nav got crowded → header .wrap widened to 1320px, mainnav + buttons tightened, and logo given flex-shrink:0 (it had collapsed to 0 width). NOTE: nav is tight (fills 1320 exactly) — watch if adding more. **FONT UPDATE 2026-07-07:** headings/body now use **Oswald** (taller/thinner condensed, per user, matching product mockup) SITE-WIDE, but **NAVIGATION stays Epilogue** (user disliked Oswald nav). Implemented by: both fonts in every page's Google Fonts link + a `header .navlink,header .dropdown a,header .dd-head,header .dd-link .t,header .cap,header .pharmacy-pill,header .hub-cap,header .cart-count,header .mobilemenu a{font-family:'Epilogue',sans-serif}` override (higher specificity beats the base Oswald). Logo=Archivo, body-text=Inter unchanged. (Old note below is superseded re: Epilogue-for-everything.)
Site fonts (OLD): **Epilogue** (display/headings — user rejected Sora, then Archivo, because they dislike a straight/stick lowercase "l"; Epilogue's lowercase l has a curved tail/foot, which fixed it. Chose via a side-by-side compare.html rendering l-heavy text across Sora/Epilogue/Cabin/Jakarta/Manrope/Space Grotesk/Familjen/Figtree). Body stays **Inter**; **Archivo** italic 900 kept ONLY for logo wordmark; Roboto Slab for tagline. User prefers white/bright text over grey — --muted brightened, hero subtext white. NOTE FONT SENSITIVITY: user cares a lot about letterform details (esp. lowercase l). (Kid-game font memo [[no-comic-sans]] does NOT apply — this is a serious brand.)

Navigation (user-specified, exact wording): Injuries, Recovery, Performance, **Sports Medicine Professionals**, Products, Learn + search icon + red "Start My Recovery" CTA. Note: "Sports Medicine Professionals" is a long label — required white-space:nowrap + tightened nav spacing to fit one line at desktop.

**Sports Medicine landing page (sports-medicine.html) — BUILT OUT to a real design** (replaced the old dark placeholder) from a user mockup. IMPORTANT: this page is a **LIGHT theme** (white/navy/red) — deliberately distinct from the dark athlete homepage. Its own pro nav: Services (active) / Products / Community / Resources / About Us + navy "Professional Login" button. Sections: navy hero ("Partnering with professionals. / Supporting athletes." red 2nd line + Explore Our Services / Contact a Rep CTAs + medical-cross accent), light trust bar (4), "How We Support You" with 5 service cards + right column (photo + "Why Professionals Choose SportPharm" 5-point checklist), navy "Ready to Work Together" band (3 features: Dedicated Support / Reliable Delivery / Focused on Outcomes), footer line "We take our drugs seriously so you can take care of your athletes." Now shows all **8 services** (updated after crawling live site — see [[sportpharm-current-site-inventory]]): Specialized Compounding, Drug Repackaging, Blister Packaging, Inventory Management, Retail Pharmacy Services, Concierge Sports Pharmacy, Supplement Support, Customized Services (its CTA = "Request a consult"). 4×2 grid, copy matches live-site wording, Services nav dropdown lists all 8. Two image slots awaiting drops → assets/pro-hero.png (pro taping ankle) + assets/pro-support.png (pro with athlete). PRODUCTS: on hold per user — placeholders OK for now; WasabiRub is the priority product to feature when the store is built.

**Entry gate:** immediate pop-up modal ("Welcome. Who are you?") on homepage load with two choices — **I'm an Athlete** (closes modal → stays on homepage) and **I'm a Healthcare Professional** (redirects to **sports-medicine.html** Pro Portal placeholder page). Per user (UPDATED): now shows **at most ONCE PER CALENDAR DAY** (localStorage key 'sp_modal_day' = date string; every-load was annoying). Add **?popup** to the homepage URL to FORCE the modal (for demos/testing). Modal logo fixed — now uses the real logo image (assets/logo-white.png) instead of the text wordmark. Runner photo now in hero (see below). Header CTA "Start My Recovery" was REMOVED (purpose undecided); replaced with search + cart icons (cart = placeholder for the e-commerce/checkout that's coming) + mobile hamburger menu.

Runner hero image: DONE — user's real sprinter photo ("homepage runner.png", dark navy bg) is now **full-bleed** in the hero (absolute .hero-photo, 57% width, bleeds to right edge, left-gradient fade into navy + red glow; NOT a contained block anymore). Lives at /tmp/sportpharm-site/assets/runner.png.

**Image slots wired with graceful fallback** (img with onerror="this.remove()" over the styled SVG/gradient placeholder). ALL 6 IMAGES NOW PLACED & live (copied from sportpharm-assets to /tmp/sportpharm-site/assets/): injury-ankle-sprain.png, injury-acl.png, injury-hamstring.png, injury-lower-back.png (Popular Conditions cards — dark bg + red-glow injury photos), body-map.png (dark anatomical figure w/ red points; mix-blend-mode:screen was REMOVED — it washed the figure out to a faint ghost and looked weird per user. Now: figure sized up to 150px wide + soft radial mask feathering the edges into the page, no blend), brandon.png (transparent RGBA cutout in navy polo). Per user, REMOVED the small boxed avatar — now a large cut-out (.brandon-figure, 360px, align-self:end, negative bottom margin) standing centered in the "Answers you can trust" band, bleeding into the section bottom (overflow:hidden) with a soft red radial glow behind. Matches a reference the user gave (big cut-out standing in the band, no frame). All confirmed loading (naturalWidth>0).
(Featured Resources thumbnails + hero popular-topic icons still placeholders — not yet requested.)

PREVIEW SCREENSHOT QUIRK: scroll-then-screenshot on this page returns blank navy frames (compositor timing). WORKAROUND: use a tall viewport (e.g. 1200x1550 or 1200x2950) and screenshot at scrollY 0 so no scrolling is needed. Drop folders: **/Users/kennadyscott/Documents/Claude/sportpharm-assets/** (user drops here, Claude copies to site) and **/tmp/sportpharm-site/assets/** (live site asset dir). ~/Documents readable by Claude's tools but NOT by the preview server, so images must be copied into /tmp/sportpharm-site/assets/.

**PUBLISHED (2026-07-07):** pushed /tmp/sportpharm-site to GitHub repo **kennadyscott/sportpharm-site** (main branch). **LIVE on GitHub Pages (clean URL): https://kennadyscott.github.io/sportpharm-site/** (sports-medicine at /sports-medicine.html). Classic branch-based Pages (main /root) enabled via API and built fine in ~15s this time (see updated [[github-auth-via-keychain]] note). Also works via raw.githack (.../main/index.html). To re-deploy after edits: git add/commit/push to the repo; Pages rebuilds automatically (~15-60s), githack caches per-file ~60s. (User's other account "kennady-scott" with a hyphen is separate; keychain token only pushes to "kennadyscott".)

**INJURIES HUB PAGE BUILT (injuries.html, 2026-07-07):** example "content driver" page (dark consumer theme, copied header/footer/nav from index.html, modal removed). Injuries nav dropdown SIMPLIFIED to 3 per user: **Browse by Body Part / Browse by Sport / Injury Library** (→ injuries.html#body-part / #sport / #library); "Injuries" navlink now links to injuries.html (index.html + mobile menu updated). Page sections: page-hero ("Find your injury." + search + 3 big driver cards), Browse by Body Part (body-map.png + body-part chips), Browse by Sport (10 sport cards), Injury Library (filter chips + 8 cond-cards: 4 real photos ankle/acl/hamstring/lower-back + 4 glow placeholders rotator cuff/tennis elbow/shin splints/concussion, each tagged by body part), and a "Now recover" BRIDGE band linking Injuries→Recovery.

**Products nav item** (added next to Learn, per user, on all consumer pages: index/injuries/ankle-sprain + new products.html) → links to **products.html**, dropdown lists WasabiRub / IcetraRub / WasabiRub Super Hot / Shop All. (This is SEPARATE from "The Pharmacy" red capsule which still → external sportpharm.com/store. Dark-page navlink padding trimmed 13→10px so the 5th item fits the 1240 nav.)

**SHOPPING PAGE BUILT (products.html, 2026-07-07):** LIGHT-theme e-commerce prototype for the **3 main products only** (per user): **WasabiRub** ($29.95, Best Seller, red panel, OTC+TruShield, 3-way Menthol/Capsaicin/Methyl Salicylate), **IcetraRub** ($39.95, cool-blue panel, OTC, 16% menthol cooling), **WasabiRub Super Hot** ($39.95, hot-orange panel, OTC, max heat). Color-codes cool(blue)/balanced(red)/hot(orange). Sections: hero ("Targeted relief you can rub right in" + pro-teams trust line), product grid (cards: gradient panel + tube icon, badges, star rating, ingredient tags, price, Add to Cart), "Which rub is right for you?" cool→hot comparison, trust band, footer. **Working add-to-cart**: increments a header cart-count badge + shows a toast (JS, no real cart page yet). **REDESIGNED 2026-07-07** to a warmer, less-clinical retail landing (2nd user mockup they preferred): DARK navy hero ("TARGETED RELIEF. PROVEN RESULTS." + Shop All Products + 3-jar lineup) → light body: 4-feature row → "OUR CORE TOPICAL RUBS" cards with colored relief-tier ribbons → Quality/Made-in-USA strip → **Ask Brandon** dark band (brandon.png in red-ring circle + quote) → Free Shipping/Easy Returns/Secure Checkout/Need Help trust bar → footer. **Product COLORS now match real branding: WasabiRub=GREEN(soothing), IcetraRub=BLUE(cooling), Super Hot=RED(maximum)** — dropped the old star-ratings/comparison/tube style. Copy per mockup. UPDATE 2026-07-07: **real product photos wired** (product-wasabi/icetra/superhot.png + product-lineup.png hero; fill card image area object-fit:cover). **Live-store PRICING now used: WasabiRub $29.95, IcetraRub $39.95, Super Hot $39.95.** **TruShield Certified badge** overhangs top-right of all 3 cards (CSS/teal recreation + img slot assets/trushield-certified.png onerror-fallback; cards set overflow:visible+position:relative, ribbon top-radius). **Hero CTA changed** "Shop All Products" → **"Which rub is right for you?"** button opening a MODAL (Cool=IcetraRub / Everyday Soothing=WasabiRub / Max Heat=Super Hot; each "Shop X" → product anchor #icetrarub/#wasabirub/#superhot, closes modal). **Hero image enlarged + bleeds over the midline** (phero-r margin-left:-80px, taller); removed the fake SVG jar fallbacks. Reduced padding above "Our Core". **PRODUCT DETAIL PAGE TEMPLATE BUILT (icetrarub.html, 2026-07-07)** from user's preferred mockup (Mockup 1, the "new imagery" one — single large product image on a themed tinted frame, NOT the thumbnail-gallery Mockup 2). LIGHT theme. Layout: breadcrumb → 2-col (left: product image in cool-tinted rounded frame + "*Packaging may vary" + "Third-Party Tested" callout box w/ red-checkbox + TruShield badge; right: OTC-category pill, name (Oswald), red subtitle "Professional Cooling Pain Relief", specs "16% Menthol • Pharmacist Formulated", Third-Party-Tested badge + 4.9/250+ stars, description, 4 feature icons, $39.95, SIZE + QUANTITY stepper, ADD TO CART + BUY IT NOW, Free Shipping/60-Day guarantee) → "Why athletes choose" 3-card band → "You may also like" related products → footer. Interactive qty stepper + add-to-cart badge/toast. Linked from products.html IcetraRub card "Learn More" + modal "Shop IcetraRub". **ALL 3 product pages now BUILT: icetrarub.html, wasabirub.html ($29.95, green, Natural/Targeted/Fast-Acting/Trusted features), superhot.html ($39.95, red, Deep Heat/Max Strength/Long Lasting/Trusted).** Products.html "Learn More" + "Which rub" modal options each link to their product page. Cross-links (nav dropdown + related) point page-to-page. FONT: product **subtitle (.subttl) uses Epilogue** per user ("sub-headings" = Epilogue; example was "Professional Cooling Pain Relief") — Oswald title + Epilogue subtitle. Product-page image frame = white (linear-gradient #fff→#f5f8fc), **object-fit:cover** (fills/crops — user wanted images to fill, esp. the wide 1200x630 wasabi shot). Wasabi/superhot pages generated via `python3` string-replace from the icetra template. LAYOUT UPDATE (per user): **TruShield badge now overhangs the product image's top-right corner** (.img-badge, CSS teal card + trushield-certified.png slot). **Removed the "Third-Party Tested for Safety" callout box; replaced with a COMPACT "Why athletes choose X" block (.whymini, 3 small icon rows, accent-colored per product) placed under the image next to the Add-to-Cart area.** Removed the old full-width "Why athletes choose" band. (Unused .tested/.trushield/.band CSS left in place, harmless.)

RESOLVED: WasabiRub image is now white-bg (user re-dropped wasabi-rub-gen-public.png on white → product-wasabi.png; all 3 product cards + pages now consistent white). TruShield real badge PNG still a slot (trushield-certified.png) if user wants the exact asset vs the CSS recreation.

**Performance nav** now includes **Training** (first item; "Prep for marathons, big rides & events" — focus on endurance/everyday-athlete event training: marathons, big bike rides). Added to index.html, injuries.html, ankle-sprain.html Performance dropdowns.

**INJURY GUIDE / DETAIL PAGE BUILT (ankle-sprain.html, 2026-07-07)** from a user mockup — the template for individual condition pages (linked from injuries.html Injury Library Ankle Sprain card). IMPORTANT THEME SPLIT (user's direction): **detail/content pages are LIGHT-themed** (white, like sports-medicine) while browse/landing pages (homepage, injuries hub) stay DARK. Light header uses **wordmark-dark.png** (new asset: red+black wordmark cropped from logo.png, no tagline) + "Sports Recovery and Performance Hub" tag, our current consumer nav (Injuries active / Recovery / Performance / Learn + The Pharmacy red capsule + Sports Medicine navy capsule). Layout = navy breadcrumb bar + 3-col grid: left Injury Library sidebar (body-part/sport nav + assessment card), center (injury hero: image+title+meta+CTAs, 9 tab strip, "Your Recovery Roadmap" 4 phase cards w/ chevrons, Featured Resources), right rail (Why Trust SportPharm / Recommended Products / Ask Brandon dark card w/ brandon.png). CSS GOTCHA fixed: center grid col needed `minmax(0,1fr)` + `min-width:0` (and .phase min-width:0) or the flex roadmap blew the column to 1168px and pushed the right rail off-screen. LAYOUT WIDENED (user said it looked squished vs mockup): --maxw 1280→**1440**, columns rebalanced to 236px/1fr/306px gap40 (center ~762px), and it now **stacks to one centered 840px column below 1300px** so laptops never cram 3 cols. Used mockup's own nav in image = OLD nav; ignored it, used our current nav.

**ALL 8 INJURY GUIDES BUILT + REDESIGNED (2026-07-10, USER-APPROVED FINAL v4):** refinements over v3 that got approval: hero flows STRAIGHT into tabs (meta strip "Common in/Typical recovery/Provider recommended" REMOVED per mockup — data still in generator dicts, can resurface in Overview tab content later); hero gradient darkened to near-black navy (radial #1a355f→#0A1728); phase icons light-red tint #f2bdb9 at 30px; quick-nav bar has NO dividers, chunkier (38px icon chips); Ask Brandon bottom card = photo as full-height right-side rounded panel (42% width, object-fit cover) not corner cutout; layout slimmed to 206px sidebar / 272px rail / --maxw 1360 (user found wider version cramped at 100% zoom on 1440 screen — hero CTAs must sit INLINE, if they stack the center is too narrow). ankle-sprain, acl, hamstring, lower-back, rotator-cuff, tennis-elbow, shin-splints, concussion (.html each). Generated from ONE python generator (scratchpad gen_guides3.py pattern — regenerate all 8 from data dicts, never hand-edit pages individually). Design v3 (user mockup): navy crumb bar → 3-col layout on soft-gray canvas (240px Injury Library sidebar card w/ red active body-part + assessment box | white center card | 312px rail w/ Why Trust + Recommended Products) → center = DARK NAVY HERO CARD (radial #1c3f76→#0B1E3B + faint grid mesh, crumbs inside, Oswald H1, lead, red Start Recovery Plan + ghost Download CTAs, injury photo bleeding bottom-right with a left-edge mask fade so it never cuts the headline) → meta strip (Common in / Typical recovery / Provider recommended) → 8 tabs (visual only) → "Your Recovery Roadmap" gray panel (4 Phase cards w/ faint corner icons + View Guide links) + dark quick-nav bar (Expert Videos/Exercises/Recovery Tools/Products/When to See a Doctor) → bottom row spanning center+rail: 4 resource cards + compact Ask Brandon navy card. Injuries.html library cards all link to guides. Sub-headings = Epilogue everywhere (meta labels, card titles, phase labels); Oswald only for H1/section heads/tabs. MISSING PHOTOS (graceful navy fallback): injury-rotator-cuff.png, injury-tennis-elbow.png, injury-shin-splints.png, injury-concussion.png. **MEDICAL DISCLAIMER on all 15 page footers** (educational-only + seek provider; hairline-divided footer block). **Epilogue sub-heading treatment applied SITE-WIDE** (trust items, condition/resource/action card titles, service cards, product card titles, why-cards etc; heroes + big section heads stay Oswald). **Interactive build tracker widget** lives in chat (click-to-build via sendPrompt); shareable static roadmap at /roadmap.html on the live site.

**HOMEPAGE FULLY CLICKABLE + RECOVERY HUB BUILT (2026-07-10):** (1) **Every homepage click lands somewhere**: topic chips→guides (Knee Pain→acl, Shoulder Pain→rotator-cuff), Popular Conditions cards→4 guides, body-map labels/tabs→injuries anchors, action bar→sports-medicine/products, footer wired. **WORKING SEARCH**: hero "What hurts?" bar has client-side JS keyword index of all pages ("my shoulder hurts from swimming"→rotator-cuff.html; "cooling cream"→icetrarub.html; fallback injuries.html#library). (2) **coming-soon.html** — branded "On the roadmap" page taking `?f=Feature+Name` (JS reads param into H1/title) + 4 live-section cards; ALL unbuilt destinations sitewide point here (Performance/Learn dropdowns, resources, Ask Brandon, legal, company). Doubles as build-priority signal. (3) **recovery.html BUILT** — dark theme (copied injuries.html head/header via python build_recovery.py in scratchpad): hero + 4 driver cards → #timelines (8 injury timeline cards w/ phase chips → guides), #exercises (8 video-category cards → coming-soon), #return-to-play (**interactive 6-item readiness checklist**, JS progress bar + verdict), #tools (4 coming-soon tool cards), bridge→products. Recovery nav item is now a real link sitewide; Recovery dropdown items → recovery.html anchors; guide quick-nav bars wired (Exercises/#exercises, Recovery Tools/#tools, Products, rest→coming-soon); guide sidebars/product-links/CTAs all wired — **zero dead anchors on any page**. Guides regenerate from gen_guides3.py which now carries ALL wiring.

**PERFORMANCE HUB BUILT (2026-07-10, performance.html):** dark theme via scratchpad build_performance.py (same injuries.html-derived pattern as recovery.html). Hero "Train smarter. Stay in the game." + 4 drivers → #training (6 event-prep plan cards: Marathon 16wk / Half 12wk / 5K-to-10K 8wk / Century & Big Rides 12wk / Sprint Tri 10wk / Race-Week & Taper 2wk — all → coming-soon Event Training Plans), #prevention (8 cards → the 8 injury guides), #strength (8 video-category cards → coming-soon), #warmup (**interactive warm-up builder**: sport select → renders 5-step protocol w/ durations + total minutes; 6 sports incl. FIFA 11+ for soccer), bridge → recovery.html. Performance navlink now a real link sitewide + dropdown items → performance anchors (generator synced). Section-hub pattern is now established: injuries/recovery/performance all dark w/ drivers + anchored sections + one interactive widget each (checklist / warm-up builder). Remaining coming-soon clusters: Learn (blog/video/Ask Brandon), commerce (cart/checkout), pro sub-pages, company/legal pages.

**8 SUBHEADING LANDING PAGES (2026-07-10, per user "each subheading should go to a landing page, not just a section"):** built via scratchpad build_landings.py (shared page() scaffold from injuries.html base, dark theme, crumb Home/Hub/Page): recovery-timelines.html (how-to-read mini-cards + 8 timeline cards), rehab-exercises.html (8 category cards WITH 3 sample exercises each + phase-matching band), return-to-play.html (interactive checklist + 8 injury checklist links), recovery-tools.html (4 tool cards w/ feature bullets), training.html (6 plan cards + "every plan includes" strip), injury-prevention.html (4 principles + 8 guide links), strength-mobility.html (8 routine cards w/ samples), warmup.html (warm-up builder + after-session strip). Dropdowns + quicknav + deep links repointed sitewide from hub anchors → landing pages; HUB pages keep internal #anchor drivers and their card grids now click through to landings. All injuries.html-DERIVED pages had inherited dead footer/mobile links — fixed sitewide sweep; also wired sports-medicine pro-nav dropdowns + Learn more cards (→ coming-soon) and products.html Ask a Question. **Site verified: zero dead anchors on all 25 pages.** LESSON: injuries.html is the base template for dark pages — fixing links there BEFORE deriving prevents inherited dead links; the final sweep regexes for hrefless/# anchors are in this session's scratchpad scripts.

**8 BODY-PART LANDING PAGES (2026-07-10, scratchpad build_bodyparts.py):** head-neck / shoulder / elbow / wrist-hand / back-spine / hip-thigh / knee / lower-leg-ankle-foot (.html). Dark, from injuries.html base. Each: crumb Home/Injuries/X + hero + "Common conditions" 4-card grid (real guides → guide pages w/ "Full guide"; unwritten conditions → coming-soon "?f=The+X+Guide" cards w/ "Guide coming soon" pill ABOVE the title — absolute-positioned pill collided with long titles) + generic "When to get checked" red-flags strip + sport chips + bridge → recovery-timelines. Taxonomy: 8 parts; hamstring lives on hip-thigh; injuries.html's 10 chips map Lower Leg/Ankle/Foot → lower-leg-ankle-foot.html. WIRED: injuries.html chips (span→a), homepage body-map labels (each to its page), guide sidebar body-part links (generator BP_URL map). Condition ideas captured per part (meniscus, patellar tendinitis, runner's knee, achilles, plantar fasciitis, golfer's elbow, UCL, impingement, dislocation, AC joint, sciatica, herniated disc, groin, hip flexor, quad contusion, whiplash, stingers, wrist sprains...) — these are the natural next guide builds. Site now 33 pages, zero dead anchors.

**MAIN SITE + PERSONA HOMEPAGES (2026-07-12):** Big restructure. The entire prior build is now the **Athlete Hub** — old `index.html` renamed to **athlete-hub.html** (all sitewide `index.html` home/logo links repointed). New **index.html** = SportPharm main-site homepage (LIGHT theme, matches user mockup) built by scratchpad **build_homepages.py** (generator emits 3 persona homepages: index.html=Everyday, pro-athlete.html=Pro, for-professionals.html=Healthcare). Fonts: **Bebas Neue (display) + Inter (body/nav)** per user (replaced Oswald/Epilogue on the MAIN SITE only; hub still Oswald/Epilogue). Sections: hero (bg photo assets/home-hero.png = user's group-run photo), "What do you need today" 4 cards w/ **user's custom colored icons** (green-screen PNGs → knocked out green, KEPT color: recover=red, train/check/find=blue; processed via PIL in-session, saved need-recover/train/check/find.png), split, "Start where you are" 3 cards, WasabiRub band (product-wasabi.png), "Sports Pharmacy explained" 3 icons, blog row, navy final CTA, dark footer w/ disclaimer. **Entry pop-up** = photo-card chooser "What brings you here?" using user's 3 baked card images (persona-everyday/pro/pros.png — full cards w/ text+arrow baked; user OFFERED clean no-copy versions → swap to HTML-overlay text later for crispness). Shows first visit (localStorage sp_persona_seen) + `?welcome` forces. **Main-site NAV** (user-specified): "What Brings You Here" DROPDOWN (3 persona links, click label opens modal) · Athlete Hub · For Professionals · WasabiRub · Articles · Contact. Footer cols include About + Services (Services→sports-medicine.html). Image drop folder now has subfolder **everyday-athlete-homepage/** (hero-image, recover/train/check/find icons, 3 ChatGPT persona cards, homepage-mockup). STILL PLACEHOLDER (navy gradient): split img (home-everyday.png), start-recover/perform/return.png, blog-1/2/3.png. **/tmp was WIPED between sessions — restored via git clone from GitHub; generators live only in scratchpad (ephemeral), repo HTML is source of truth.** launch.json "sportpharm" re-added (python http.server /tmp/sportpharm-site :4184).

**MAIN SITE POLISH + NEW PAGES (2026-07-12 cont.):** Nav restyled to CAPSULES (build_homepages.py header): plain links [What Brings You Here ▾ · Athlete Hub · Articles · Contact] + divider + red "WasabiRub" pill (icon+glow) + navy "For Professionals" pill. Hero updated to match user mockup: BIG Bebas headline (5.7rem) directly over photo w/ lighter diagonal gradient + text-shadow (was a heavy dark panel; also fixed a flex `.hero .wrap{width:100%}` bug that collapsed/centered the copy). Real Everyday imagery all wired: hero=home-hero.png (group run), split=home-everyday.png (man stretching), start cards recover/perform/return, need-card icons (user green-screen PNGs, green knocked out keeping color: recover=red, others=blue), WasabiRub band=**wasabi-band.png** (lifestyle jar+gymbag+towel, full-bleed right; was in Downloads not the folder), pharmacy-explained icons recolored (med=red, supplement+clean-sport=blue). Logo tagline changed to "Stay ready for whatever moves you." (main site only; hub keeps its own). **3 BLOG ARTICLES + articles.html index** built (build_articles.py imports build_homepages; real copy w/ ACSM/CDC/FDA citation chips, category chip, Bebas title, banner, closing CTA band, Keep Reading); homepage blog cards + Articles nav wired to them (blog-recovery/nsaids/return.png). **WASABIRUB.HTML replaced** with user-provided "FEEL IT WORK" landing (self-contained, dark, triple-action selector w/ JS tabs, external i0.wp.com product imgs, CTAs → sportpharm.com store) — nav capsule + homepage buttons all land here. **SPORTS-MEDICINE.HTML rebranded** to Bebas Neue + Inter (swapped Oswald/Epilogue, bumped heading sizes, capsule pills now Bebas uppercase); "The Pharmacy" pill + logo + "Main Site" now → index.html (main homepage). **"For Professionals" capsule + Healthcare Professional persona (modal+switcher) now → sports-medicine.html** (for-professionals.html persona page now orphaned but still generated). OUTSTANDING: (1) user requested a **compress-to-KB pass** on all imagery (~2MB PNGs) — not done yet; (2) Pro/Healthcare persona homepages still on placeholder photos; (3) pro-hero.png/pro-support.png still missing on sports-medicine.

**EA HOMEPAGE REDESIGN + ADMIN CONSOLE (2026-07-24):** (1) Rebuilt **index.html** (Everyday Athlete) from a user-provided zip (single-page pathway experience: hero "BUILT FOR THE WAY YOU MOVE." Roboto Condensed/Arial Narrow, "What brought you here" 4 pathways, "you belong here", "Push Through or Stop?", Recover/Rebuild/Return, WasabiRub, Sports Pharmacy Explained, articles, CTA). Restored the real logo (wordmark-dark.png) + "Stay ready for whatever moves you." + the established main-site nav; tightened section padding. **Brand red fixed** — redesign shipped off-brand #ed2029; changed `--red` to **#E0312A** site-brand red (fixes WasabiRub capsule + all accents). (2) **PERSONA TOGGLE** replaces the old persona dropdown/"For Professionals" capsule — a segmented switch **Everyday Athlete · Pro Athlete · Healthcare / AT** (routes index/pro-athlete/sports-medicine, active state filled navy) placed on ALL 3 persona pages; on the EA page "What Brings You Here ▾" now drops to the 4 pathway landing pages (#stop/#recovery/#pharmacy/#belong — repoint when dedicated pages built). Toggle hides <820px on sports-medicine (collapses to menu). (3) **New hero** = assets/ea-hero.jpg replaced with `new-hero.png` (same waterfront-run scene, awkward lunging guy removed). Added 4 lifestyle shots into repetitive sections (ea-climber/ea-soccer/ea-yoga into "you belong here" collage; ea-gym into Rebuild step). Assets in **sportpharm-assets/everyday-athlete-homepage/** (+ `lifestyle imagery/` subfolder); new-hero.png at sportpharm-assets root. (4) **ADMIN CONSOLE PROTOTYPE built: /admin.html** (LIVE, pushed). Decision: **prototype-first** (user chose over standing up real Payload now) — a fully clickable, localStorage-backed console that becomes the blueprint for the eventual Next.js + Payload build. On-brand (navy sidebar + red active, Bebas + Inter). Demo login (any creds, sessionStorage auth). Modules (all 4 requested): **Dashboard** (KPI cards + SVG area/donut charts + recent articles/leads), **Blog & Content** (full CRUD — editor with title/auto-slug/excerpt/markdown-lite body, category select, tag chips, author, draft/published toggle, publish date, featured image URL+upload-to-dataURL; list w/ status filters + search + delete), **Products** (table + add/edit modal, seeded WasabiRub/IcetraRub/Super Hot + 5 more, active/out/draft), **Leads** (table + detail drawer w/ persona/status/message + status seg + mailto reply; seeded 5), **Analytics** (traffic/top pages/audience split/top articles), **Settings** (brand + reset demo data). Single self-contained HTML w/ vanilla hash router; seed reloads if localStorage empty (so each browser starts pristine). All charts inline SVG (no libs). GOTCHA fixed: SVGs in full-width buttons need explicit `.btn svg{width:16px}`. NEXT: wire the public Contact/"Contact a Rep" forms to push leads into sp_admin_leads; build the 4 pathway landing pages; then port models to Payload.

**⚠️ WASABIRUB REDESIGN REVERTED (2026-07-24) — READ THIS FIRST.** The multi-page site + green rebrand + red re-skin below were ALL **rejected by the user** ("I just don't like what you've done with the layout at all… this looks awful"). **Current state of `wasabirub.html` = the user's ORIGINAL approved single-page "FEEL IT WORK" mockup** (they re-sent it: Downloads/WasabiRub-Landing-Page.html), restored verbatim with only two changes: (1) header logo swapped from my WasabiRub wordmark → **SportPharm wordmark (assets/wordmark-dark.png) + tagline "Perform. Recover. Return."** (red periods, .brand-tag); (2) product images point to local **assets/wr-{wasabirub,icetrarub,superhot,duo}.png** (transparent, white knocked out) instead of external i0.wp.com. Footer logo → assets/wordmark-white.png. The whole **/wasabirub/ folder was DELETED**, the 57 main-site links repointed back to `wasabirub.html`. Original palette (KEEP): `--red:#d6202a` (WasabiRub's own red, NOT the site #E0312A), `--deep:#101820` hero, `--ink:#15202b`, tier accents blue #337fa7 / green #27865a / orange #c85b38, red announcement bar + red final CTA, native Arial/Helvetica weight:1000 display, GREEN product glow behind the hero jar. LESSON: do NOT redesign WasabiRub's layout or rebrand its colors; the FEEL IT WORK single page is the approved design. A multi-page wasabirub.com is still on the Director's roadmap but must be built FROM this mockup's exact look, not a new system, and only when the user asks. (Historical detail of the rejected build kept below for reference.)

**WASABIRUB MULTI-PAGE SITE (2026-07-24, /wasabirub/ folder — ❌ REVERTED, see above):** Per Director-of-Marketing priority list, turned the single wasabirub.html "FEEL IT WORK" landing into a self-contained multi-page site (its own site → future **wasabirub.com**; main site just links to it). Built by scratchpad **build_wasabirub.py** (shared SHELL/nav/footer + per-page content, cart JS). Pages: **index.html** (Home — green/ink hero, testimonial strip, lineup 4 cards, triple-action, OG Heat Duo bundle, CTA), **find-your-rub.html** (product page with custom **dropdown** switching WasabiRub/Super Hot/IcetraRub/LidoRub-coming-soon → image+tier+actives+price+add-to-cart, deep-links via #id, comparison table), **how-it-works.html** (triple-action science + 4-step usage + OTC safety), **professional.html** (teams/trainers/clinics; CTA→../sports-medicine.html), **athlete-hub.html** ("transfer blogs for now" — 3 cards → ../article-*.html + ../injuries.html), **cart.html** (localStorage cart: line items, qty steppers, promo field w/ demo codes FEELIT15/WELCOME10/FREESHIP1, free-ship-over-$75, Stripe-badged checkout stub, empty state). **Design (Director style fixes):** ⚠️ **PALETTE LESSON — do NOT make WasabiRub green.** First pass went green-forward (wasabi=green + "trimmed palette") and the user reacted VERY strongly ("why is it green?! you've changed the look and feel of EVERYTHING"). REVERTED same session to the original **FEEL IT WORK red-on-dark** identity: `--green` var now holds **#E0312A** (SportPharm brand red; despite the var name), neutral near-black darks (--ink #161d26, not green-biased), cool-neutral --paper #f4f5f7, --coral #e24a2b sibling accent, --navy #0B1E3B for the "For Athletes" capsule. WasabiRub's look = dark heroes + red accents + white "FEEL IT"/red "WORK." + red pill CTAs. "Trimmed palette" meant FEWER colors (drop the old blue/orange/green clutter), NOT a hue change. (Regenerate via build_wasabirub.py; the green→red swap map is in this session's scratchpad.) **pill/rounded buttons** (border-radius:999px); **transparent product images** — the store PNGs (i0.wp.com wasabirub-og/icetrarub/super-hot/Duo-Bundle trushield) had baked WHITE backgrounds, knocked out to transparent via PIL ImageDraw.floodfill from edges + quantize(200,FASTOCTREE) → 27–69KB each, saved to wasabirub/assets/. Fonts: heavy "Helvetica Neue"/Arial 900 display (per user's earlier native-heavy preference — NOT Bebas/Archivo) + Inter body. **NAV (user-refined, mimics main site, NOT the persona pattern):** WasabiRub wordmark (leaf SVG + "Wasabi"ink+"Rub"red) · text links **Find Your Rub ▾ · Athlete Hub · How It Works** (Title-case medium, not shouty) · cart icon · **"For Athletes" deep-navy (#0B1E3B) capsule → ../index.html in a NEW TAB** (replaces the old red WasabiRub capsule; no persona toggle, no Home/Professional in bar). Cart badge = localStorage wr_cart count sitewide; addToCart()/toast shared. **Wired:** repointed all **57** main-site `href="wasabirub.html"` → `wasabirub/index.html` across 38 files; old **wasabirub.html is now a meta-refresh redirect** to wasabirub/index.html. **ARCHITECTURE DECISION (answered user):** NO WooCommerce needed — it's WordPress-only; the custom Next.js+Payload stack hosts everything (catalog/content/coupons/orders as Payload collections + our own analytics) and **Stripe** runs payments + native Promotion Codes; Mailchimp for email + abandoned cart. So "WooCommerce-equivalent" = Payload + Stripe + Mailchimp. **REMAINING from Director list:** confirm/build real Stripe+coupon+Mailchimp integrations (prototyped UI only); style "trimmed color palette" done; sportpharm.com redesign notes (more functional for everyday/brick-and-mortar, add Store to menu → wasabirub.com, move testimonials up, ease of use) — NOT started. LidoRub (lidocaine 4%) is the coming-soon 4th rub.

**IA NOTE (Injuries vs Recovery overlap):** user flagged overlap. Recommendation given: KEEP BOTH but with clear intent split — **Injuries = "what's wrong" (discover/identify: browse by body part/sport, injury library, guides)** vs **Recovery = "how to heal" (timelines, rehab, return-to-play, tools)**. The simplified 3-item Injuries dropdown already resolves most overlap; injury guides bridge into Recovery. (Awaiting user's final call on merge vs keep.)

Homepage prototype BUILT (page 1): high-fidelity static HTML at **/tmp/sportpharm-site/index.html**, served via launch.json config name "sportpharm" on **port 4184** (python http.server; ~/Documents unreadable to preview per [[preview-servers-tmp-pattern]]). Self-contained custom CSS (no Tailwind CDN), portable into the Next.js build later. Sections: sticky header w/ hover mega-dropdowns, hero (eyebrow + "Recover smarter. Perform stronger." w/ red periods + big "What hurts?" search + popular topics + runner-silhouette art), trust bar (4), Find help (body-map + browse tabs + Popular Conditions cards), Featured Resources (VIDEO/PDF/ARTICLE/CHECKLIST badges), Brandon Welch quote band, 4-up action bar, footer. Injury imagery is placeholder red-glow panels; real photos + real logo asset TBD.

Requirements: **e-commerce / shopping + checkout** is a confirmed need (products are sold on-site — plan for Payload + Stripe or a headless-commerce integration when we get there). Significant custom functionality (custom web tools like calculators/dashboards/configurators + user accounts/portals) AND a blog that employees can submit articles to (draft → review → publish workflow). Maintained day-to-day by the user (technical).

Decided against Squarespace (can't do custom app functionality) and WordPress (fights you once real logins/custom tools are needed). **Recommended stack: Next.js + Payload CMS + Postgres, hosted on Vercel** (auto-deploy from GitHub). Payload runs inside the Next.js app, provides auth/roles/admin panel — covers employee blog submission workflow natively.

Planned build sequence: (1) scaffold + deploy empty shell, (2) design system, (3) marketing pages, (4) blog + submission workflow, (5) custom tools & portals. Building page by page.

Open before build starts: what SportPharm actually is; whether there's existing brand/design direction. See [[no-comic-sans]] for font preferences.

**CONTENT STUDIO REVIEW DASHBOARD (2026-07-17):** built a marketing-director-facing review dashboard for the 10 proposed content strands (from the site crawl: TruShield/clean-sport lead strand, Sideline Ready, compliant dispensing, recovery evidence-vs-hype, topicals, sport-specific playbooks, meet-the-team, since-1995 social proof, seasonal, nutrition/supplement science). Each strand: pitch + cadence + spin-off list + fully drafted LinkedIn caption rendered in a LinkedIn-style mockup (CSS-drawn brand visual card, navy+red, "Draft mockup · not published" label). Review controls: notes textarea + Approve/Request changes/Reset (localStorage, per-browser), scoreboard, "Copy feedback summary" → clipboard digest. **Published as Claude artifact: https://claude.ai/code/artifact/9e592314-6e04-4974-a004-ac97661f45a8** (source: session scratchpad sportpharm-content-studio.html; local copy served from /tmp/sportpharm-content via launch.json "sportpharm-content" port 4193). Director feedback flow = they click share on the artifact or user shares it; notes come back via the copy-summary digest.

**v3 (2026-07-17): full campaign briefs.** Each strand is now a named CAMPAIGN (user-specified structure): Campaign Platform / Strategy (primary goal + supporting + audiences) / Message Hierarchy (Emotional·Educational·Product·Brand) / Campaign Structure (3 phases) / Supporting Stories / Platform Adaptations (channel notes + the asset feed) / Landing Page Structure / Visual Direction / Claim & Compliance Guardrails / Metrics. Campaign names: **SportPharm Clean Sport Campaign** (user-named), Sideline Ready, Compliance Playbook, Recovery Honestly, Targeted Relief, Injury Playbooks, Behind the Counter, Trusted Since 1995, In Season, Read the Label. Sticky section jump-nav per campaign; per-SECTION "+ Note" feedback (keys spcs2-<sid>:sec:<key>) included in export. **REAL LOGO embedded as base64 data URIs** (CSP-safe): quantized-64-color PNGs generated via PIL from sportpharm-assets logo — white-recolored wordmark crop (navy topbar + dark post visuals + reel phone), original-color wordmark crop (light visuals); tokens __LOGO_*__ substituted by a python pass into scratchpad + /tmp copies (b64 files live in /tmp/sportpharm-content/*.b64). Guardrails are campaign-specific (Rx promotion rules for KetoRub, FTC testimonial rules, DSHEA for supplements, no-guarantee language for certification).

**v5 (2026-07-21): per-campaign share links + WasabiRub DTC campaign.** User found the Supabase/backend route too complicated → added simple **per-campaign deep links** `?c=<id>` that render a focused single-campaign view (rail + multi-campaign lede hidden, scoreboard + export scoped to that one campaign, "Copy review link" button in the header). No backend needed; each link opens one campaign for a reviewer. The 10 IDs: clean-sport, sideline-ready, dispensing, recovery, topicals, playbooks, team, trusted, seasonal, nutrition. NEW 11th campaign per President's directive to **pivot off Amazon to direct sportpharm.com sales** (Amazon labeling issues; internal-only, never in public creative): **"SportPharm Feel It Work Campaign"** (id **wasabi-direct**, link `?c=wasabi-direct`), hero WasabiRub + push IcetraRub & Super Hot. 12 assets (hero reel, lineup/triple-action/find-your-rub carousels, product-spotlight + bundle + proof + buy-direct statics, Ask Brandon interview, 3-email sequence, Stories). Grounded in live product facts: WasabiRub $29.95 triple-action (menthol/capsaicin/methyl salicylate, "only triple-action sports topical," TruShield, pro/collegiate teams), IcetraRub $39.95 16% menthol, Super Hot $39.95; bundles Fire&Ice $64.99 / OG Heat $59.99 / Recovery $64.99 / Team Trifecta $269.95 / WasabiRub Team $240.95. Guardrails: OTC external-analgesic language only (no cure/heal), no league-endorsement implication, don't name/knock Amazon publicly, soften "only triple-action" claim. mockStatic eyebrow now parameterized via asset.eye. Header now "11 campaigns · 62 assets."

**v6 (2026-07-21): WasabiRub Offers & Incentives + conversion feedback.** Added an **Offers & Incentives** brief section (new SECTIONS key "offers", buildOffers; renderDetail now filters empty sections so it only shows on campaigns with camp.offers). Offer suite: primary first-order welcome offer **FREESHIP1** / **WELCOME10** (email/SMS capture lever) + stack **FEELIT15** (launch), **LASTCALL** (Week-5 deadline close), free-ship-over-$50 (AOV), **COMEDIRECT** (marketplace win-back), Give-15/Get-15 referral, **TEAM15**, **AGAIN10** (repeat). Codes are suggestions to set up in-store. Applied user conversion feedback: (6) landing hero now leads with differentiator — h "Three sensations. One rub." + tighter dek + A/B note (buildLanding renders landingHero.note); (7) Week-5 real urgency — Post 10 reworked to "launch pricing ends Sunday" deadline close (LASTCALL); (8) abandonment elevated to core — new asset w14 3-part cart-abandon email flow (reminder→objection/review→incentive) + browse/post-purchase flows, "build before launch." WasabiRub campaign now 14 assets; header "11 campaigns · 64 assets." .code coupon-chip CSS added.

**v13 (2026-07-21): admin activity dashboard + clickable landing CTA.** Landing-page banner redesigned into a big **clickable red button** (`.lp-cta` whole-box `<a>`, hover lift, "Open preview ↗"). Added **Admin activity dashboard** — "Admin" button in header (shown when signed in; currently visible to all signed-in users, not gated) opens an overlay (#admin) showing: **people table** (name/email, last seen, visits, notes, replies, actions — sorted by last seen) + **recent activity feed**. Powered by: **presence** upsert on each signed-in load (`presence:<email>` kv = {email,name,firstSeen,lastSeen,visits}), a capped **__activity** log (last 250, {name,email,action,target,ts}) written by logActivity() on note/reply/owner/asset-status actions, and note counts scanned from `*:thread:*` in cache. adminHTML/openAdmin/closeAdmin in admin.js. Only meaningful when LIVE (signed in). To restrict Admin to just Kennady, gate the button on sessionEmail.

**v12 (2026-07-21): notes → attributed comment threads + owner dropdowns.** Owner assignment is now a **dropdown** (OWNERS=["Brandon","Jessie","Kennady"], alphabetical) instead of free text; saves on change. All note surfaces (section "+ Note", per-asset notes, overall campaign notes) converted from single autosave textareas into **comment threads**: each note has an explicit **Save note** button, shows **author** (displayName() maps signed-in email→Brandon/Jessie/Kennady) + **timestamp**, and supports **one-level replies** (each reply attributed too). Stored per thread under key `<sid>:thread:<tid>` (tid = sec:<key> / asset:<aid> / strand) as {items:[{name,email,text,ts,replies:[...]}]}. threadfns.js: commentThreadHTML/bindOneThread/bindThread/refreshThread/threadText/displayName/fmtTime. "+ Note"/"+ Add note" buttons now show 💬N count (updates live via refreshThread). Export (Copy feedback summary) uses threadText (Name: text + ↳ reply lines). Old .notes autosave removed. Author comes from Supabase session — so this is meaningfully live only when signed in.

**v11 (2026-07-21): dates + task owners + collapsible weeks.** Top-of-campaign **campMetaHTML** bar (all campaigns): Campaign start date / end date (date inputs) + Extension strategy (textarea) → store `<sid>:__meta`. Weekly Calendar weeks now native **collapsible `<details>/<summary>`** (open by default, chevron rotates). Each calendar item has an inline **Owner** assignment input (68 items) → store `<sid>:__assign` keyed `w{wi}d{di}i{ii}`; bindCalendar wires them. All persist via the same store (localStorage now / Supabase when connected). **CONNECTION STATUS: LIVE (2026-07-21).** Supabase CONNECTED — project **vleudvlmvnuvoipgcmfc** (url https://vleudvlmvnuvoipgcmfc.supabase.co), anon key pasted into SB_CFG at top of index.html + pushed. Verified: Supabase JS client inits, `kv` table reachable (select → 200, RLS active), no console errors, login overlay shows ("Sign in required"). Site is now **auth-gated, shared-persistence mode — demo/localStorage mode is gone**. **AUTH SWITCHED magic-link → email+password** (signInWithPassword; login card has email+password fields; wireLogin updated). Two accounts created by user in Supabase dashboard: **brandonw@sportpharm.com** and **jessiet@sportpharm.com**, shared password **SportPharm2026** (auto-confirmed). Added a **Sign out** button in the header (shows when LIVE → sb.auth.signOut()+reload). Recommend user turn OFF "Allow new users to sign up" to lock to just created accounts. Claude does NOT test-login as them (won't authenticate with their passwords). User must sign in with their email (one-time link) to use it; team members sign in the same way (open signups still on unless they hardened per SETUP.md optional step). Redirect URL/Site URL must be set to the Pages URL in Supabase Auth → URL Configuration (step 3) or magic-link won't return. localStorage demo data does NOT migrate. To change/rotate keys: edit SB_CFG + push. DOMAIN Q: buying kennadyscottco domain is optional/cosmetic — can CNAME it onto the existing GitHub Pages site (free, ~10 min) for a branded URL, but persistence is Supabase's job and independent of the domain/host.

**v10 (2026-07-21): Results & ROI tracker (all campaigns).** New **Results & ROI** section (SECTIONS key "results" after Metrics; buildResults/recomputeResults/bindResults; shows on every campaign, not empty-skipped) — editable per-channel metrics table (6 rows: organic posts, Stories, email, Meta ads, creators, site; cols: Spend, Clicks/Sessions, Orders, Revenue) with a live-computed navy **ROI rollup card**: total Revenue/Spend/Orders, blended ROAS (rev÷spend), and a Gross-margin% input → Estimated ROI ((rev×margin−spend)÷spend). Inputs save to store key `<sid>:__metrics` (same localStorage-now / Supabase-when-connected model). NOTE for user: in current **demo mode all notes + metrics save to the reviewer's own browser only** (per-device, not shared) — they only sync/persist centrally once Supabase is connected per SETUP.md.

**v9 (2026-07-21): WasabiRub Weekly Calendar.** Added a **Weekly Calendar · Mon–Sun** section (new SECTIONS key "calendar" after Structure; buildCalendar + TAGLABEL; empty-skip keeps it WasabiRub-only) — a full 5-week × 7-day operating schedule with per-day, channel-tagged items (colored .tg chips: Post/Reel/Stories/Email/Meta Ad/Creator/Offer), a legend, an "always-on" flows block (cart-abandon, FREESHIP1, ads, daily Stories, creator codes), and a week-header (label + focus). Ties every asset/email/ad/offer to a day (Wk1 launch→Wk5 Sunday LASTCALL-midnight deadline). 35 day rows / ~68 items. CSS in css-add-cal.txt.

**v8 (2026-07-21): creator outreach kit + section attachments + embedded landing page.** Creator section now has a copy-ready **Outreach kit** (Instagram DM, email template, one-page brief with OK-to-say / don't-say / FTC disclosure) — kitBlock() renders each as a <pre> with a Copy button (data-kit id, bound in renderDetail). Added a **reusable section-attachment upload zone** (attachHTML/bindAttach/refreshAttach + fileChips helper, refactored out of finalsHTML) — first used on the **Supporting Stories** section (key "stories"), ungated (unlike per-asset finals which need campaign approval). One-line to add to any other section. Added campaign-level **landingUrl** field: WasabiRub = https://wasabirub-campaign.kennady-nickell.chatgpt.site/ — shows a "Potential landing page" banner at the top of the detail body AND an **embedded live iframe preview** at the top of the Landing Page Structure section (.lp-preview; target has no X-Frame-Options so it frames fine). WasabiRub campaign now 14 assets + 13 sections.

**v7 (2026-07-21): WasabiRub goal-reframe + Meta Ads + Creator cross-promo.** Reframed the whole WasabiRub campaign away from "lead capture is the point" to **direct sales / traffic-to-purchase** (President's actual goal): primary goal, offers intro, first-order offer (now "First-order incentive," capture = byproduct), email adaptation, and metrics ("Owned audience"→"Remarketing & retention"). Added two new brief sections (empty-skip filter keeps them WasabiRub-only): **Paid Ads · Meta** — 3 true/dark-post ads rendered as FB/IG ad-preview mockups + targeting spec tables: (1) "Feel It Work" cold prospecting reel, (2) "Why It Feels Different" mid-funnel carousel, (3) "Trusted on Pro Sidelines" retargeting proof+LASTCALL deadline; Meta compliance note (no personal-attribute copy, OTC claims, pixel/CAPI). **Creator Cross-Promotion** — past trainers/athletes do an IG Story with a link sticker + unique code (e.g. COACHJEN15, 15%); who-to-approach list, example Story mockup, mechanics table (comp/tracking/disclosure/whitelisting/outreach), FTC + Meta Paid-Partnership disclosure required. Added "Creator / affiliate codes" row to the offer stack. New renderers metaAdHTML/buildPaidAds/creatorStoryHTML/buildCreators. Sections now 13 for WasabiRub (Meta Ads + Creators between Adaptations and Landing).

**v4 (2026-07-21): LIVE on GitHub Pages + Supabase-ready + asset uploads.** Per user request to "upload assets directly into the site," moved OFF the Claude artifact (artifact CSP blocks external network → no uploads/backend) onto **GitHub Pages: https://kennadyscott.github.io/sportpharm-content-review/** (repo **kennadyscott/sportpharm-content-review**, classic branch Pages via API, index.html + SETUP.md). Architecture chosen (user picked GitHub Pages host): static HTML that talks directly to **Supabase** from the browser (Supabase JS v2 via jsDelivr CDN) — Storage bucket `finals` for uploaded creative + a `kv(k text pk, v jsonb, updated_at)` table for shared review state (mirrors the old localStorage `spcs2-<key>` model exactly, so port was a drop-in: `store.get/set` now read a cache hydrated from kv, write-through upserts). Magic-link auth (invite-only), Postgres realtime so director's approvals sync to user's screen. **Runs in DEMO MODE (localStorage + session-preview uploads) until user pastes SB_CFG url+anon key** near top of index.html script → then LIVE. Full setup steps in repo SETUP.md (create project, run SQL for kv table + RLS + `finals` bucket policies, disable public signups, add reviewer emails, paste keys). "Finals alongside mockups" (user's scope choice): each asset card has a drag/drop upload zone UNDER the mockup showing uploaded image thumbnails/doc chips; gated behind campaign-direction approval like the other asset controls. Header shows a live/demo mode badge. GitHub token from keychain per [[github-auth-via-keychain]] (no workflow scope → used branch-Pages API, not Actions). Canonical source: /tmp/sportpharm-content/index.html (+ datalayer.js, finals.js, css-add2.txt build pieces); the older Claude artifact (artifact/9e592314-...) is now superseded by the Pages URL.

**v2 (same day): per-asset review feeds.** Each strand now opens a vertical asset feed with **4 drafted assets** (40 total): LinkedIn post (li-card mockup), Instagram post (ig-card, square visual, ring avatar), **Reel** (9:16 phone-frame cover + timed script/shot-list beats with ON SCREEN notes), and blog article (hero headline + dek + H2 outline + ending note). Each asset has its own Approve / Request changes / Reset + collapsible notes; strand-level "overall notes" box kept at bottom. Rail shows per-strand progress (n/4 + 4 status dots); scoreboard counts assets; "Show pending only" filter for second passes; export digest groups by strand → asset. localStorage namespace **spcs2-** ("spcs-" v1 keys abandoned). UX rationale (told to user): vertical feed beats grid/kanban because mockups have mixed aspect ratios and directors review by scrolling.
